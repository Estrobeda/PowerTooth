# PowerTooth host defaults embedded at compile time.
# Every value can still be overridden with the corresponding CLI option.
PROTOCOL_PREFIX=PT/1
DEVICE=/dev/ttyACM0
BAUD=115200
CONNECT_DELAY_MS=1000
HANDSHAKE_ATTEMPTS=3
INTERVAL_SECONDS=10
PAIR_TIMEOUT_SECONDS=30
